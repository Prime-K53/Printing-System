# Proxy API requests to the backend when VITE_API_URL is not set.
# If VITE_API_URL is configured in Netlify, this redirect is bypassed.
/api/*  https://primebooks-erp.onrender.com/api/:splat  200!
/*  /index.html  200
